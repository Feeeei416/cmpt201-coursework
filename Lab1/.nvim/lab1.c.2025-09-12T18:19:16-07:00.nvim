#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
  char *line = NULL;
  size_t len = 0;
  ssize_t nread;

  printf("Please enter some text:\n");

  nread = getline(&line, &len, stdin);

  if (nread != -1){
    printf("Tokens: %s\n", line);
  }

  free(line);
  return 0;
}
